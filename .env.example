# Backend API URL — update this to your deployed backend URL for production
VITE_API_URL=http://localhost:3000

# Sentry (optional — leave blank to disable error tracking)
VITE_SENTRY_DSN=
VITE_SENTRY_ENVIRONMENT=development
